import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omniguard.app',
  appName: 'omniguard',
  webDir: "out", // ⬅️ INI YANG PENTING
};

export default config;
